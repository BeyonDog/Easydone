---
description: easydone Git 提交与 PR 规范
alwaysApply: true
---

# Git 与 PR

## Commit

- **仅**在用户明确要求时创建 commit
- 提交前并行查看：`git status`、`git diff`、`git log -1`（对齐仓库 message 风格）
- Message：1–2 句，说明 **why** 而非罗列 what
- 不提交 `.env`、密钥、`keys/*.key` 等敏感文件
- **禁止**：改 `git config`、force push main/master、`git push --force`（除非用户明确要求）
- **amend** 仅当：用户要求 amend，或 hook 改文件且 HEAD 是你刚提交且未 push

PowerShell 提交示例：

```powershell
git commit -m @"
fix(gmt): 钥匙耐久只写 additional_info

GMT ItemInitParams 无 init_durability 字段。
"@
```

## Push 与 PR

- 不主动 push；用户要求时用 `git push -u origin HEAD`
- 创建 PR 用 `gh pr create`；先 `git status` / `git diff` / `git log` 了解分支差异
- PR body 含 Summary 与 Test plan
